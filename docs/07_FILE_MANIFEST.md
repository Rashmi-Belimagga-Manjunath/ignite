# File Manifest

```
ignite/
├── index.html                      Vite entry — title/meta/fonts (Space Grotesk, Inter, JetBrains Mono)
├── package.json                    name: ignite; Vite 8, React 19, sql.js, marked, dompurify
├── vite.config.js                  @tailwindcss/vite plugin; base: "./" (works on Pages subpaths)
├── .gitignore                      node_modules, dist, .env, .env.local
├── public/
│   ├── favicon.svg                 IGNITE flame mark
│   └── ignite.db                   ★ real SQLite business DB (generated by npm run seed)
├── scripts/
│   ├── seed.mjs                    builds ignite.db (profile, products, inventory, locations,
│   │                               operations, historical_sales, brand) — 262 sales rows
│   └── smoke.mjs                   Node SSR end-to-end test (DB → full 5-agent pipeline)
├── docs/
│   ├── 00_PROJECT_LOG.md           work log
│   ├── 01_OVERVIEW.md              the pitch + assignment mapping
│   ├── 02_ARCHITECTURE.md          system diagram + layers + data flow + security
│   ├── 03_AGENTS.md                the five agents + contracts
│   ├── 04_DATA_LAYER.md            SQLite schema, live APIs, MCP layer, evidence model
│   ├── 05_FRONTEND.md              routes, components, design language
│   ├── 06_SETUP_AND_DEPLOYMENT.md  run + deploy + smoke test
│   └── 07_FILE_MANIFEST.md         this file
├── src/
│   ├── main.jsx                    React root
│   ├── App.jsx                     router: / /operations /data /agents /artefacts /chat /settings
│   ├── index.css                   Tailwind v4 theme, animations, .md markdown styles
│   ├── lib/
│   │   ├── config.js               ORG meta, LS keys (ig_apikey, ig_model), demoClient "Mori Coffee"
│   │   ├── db.js                   sql.js loader (Node/browser), queryDb/queryRows/getTableList
│   │   ├── mcp.js                  ★ MCP registry — business-data + live-signals; callTool/listTools
│   │   ├── llm.js                  OpenAI streaming client, SSE, tool calls, hasKey/NoKeyError
│   │   ├── agents.js               ★ the five agents: persona, scope, tools, system prompts
│   │   ├── orchestrator.js         ★ runPipeline, checkpoints, handoffs, DEMO_BRIEF,
│   │   │                           runResearcherLiveTools, runManagerVerification, cleanOutput
│   │   ├── chat.js                 IGNITE COMMAND (runGeneral/runFollowUp), INTAKE_STEPS, DEMO_TRIGGER
│   │   ├── evidence.js             dbEvidence + computeConfidence
│   │   └── store.js                pub/sub run store (shared across pages)
│   ├── hooks/
│   │   └── useRun.js               start/approve/revise/stop; streams run + events + result
│   ├── components/
│   │   ├── Navbar.jsx              nav + logo
│   │   ├── Footer.jsx              footer with source links
│   │   ├── Markdown.jsx            marked + DOMPurify renderer
│   │   ├── PipelineFlow.jsx        animated 5-node chain diagram
│   │   ├── RunTimeline.jsx         per-agent timeline, tool checklist, progress, checkpoints
│   │   ├── EvidencePanel.jsx       live-data evidence rows + confidence bar
│   │   └── PrototypeFrame.jsx      extracts ```html fence → sandboxed iframe + code tab
│   └── pages/
│       ├── Home.jsx                hero, MOVES cards, live-data proof, demo transcript
│       ├── Operations.jsx          demo centre — split-screen live run
│       ├── LiveData.jsx            MCP console + SQL explorer
│       ├── Agents.jsx              flip-card roster + chain
│       ├── Artefacts.jsx           five deliverables as tabs + downloads
│       ├── Chat.jsx                IGNITE COMMAND chat + narrator + live pipeline
│       └── Settings.jsx            API key + model
└── .github/workflows/pages.yml     Pages deploy (build secret VITE_OPENAI_API_KEY)
```

Line counts (approx): `src` ~3,300 lines; `scripts` ~300; generated `ignite.db` 57 KB.
